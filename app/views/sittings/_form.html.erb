<%= form_for(sitting) do |f| %>
  <% if sitting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sitting.errors.count, "error") %> prohibited this sitting from being saved:</h2>

      <ul>
      <% sitting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>

  <div class="field">
    <%= f.label :start_time %>
    <%= f.time_select :start_time %>
  </div>

  <div class="field">
    <%= f.label :stop_time %>
    <%= f.time_select :stop_time %>
  </div>


  <div class="field">
    <%= f.label :number_of_kids %>
    <%= f.number_field :number_of_kids %>
  </div>

  <!-- <div class="field">
    <%= f.label :billed_amount %>
    <%= f.number_field :billed_amount %>
  </div> -->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
